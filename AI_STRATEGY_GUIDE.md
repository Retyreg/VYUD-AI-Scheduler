# AI Strategy Generator - Руководство пользователя

## 🎯 Обзор

AI Strategy Generator - это мощный инструмент для автоматической генерации месячных контент-планов с использованием Google Gemini AI. Вместо ручного создания каждого поста, AI анализирует ваш продукт, целевую аудиторию и автоматически создает стратегию публикаций на весь месяц.

## ✨ Возможности

- 🤖 **Анализ продукта и аудитории** - AI глубоко анализирует ваш бизнес
- 📊 **Автоматическая стратегия** - Создание оптимальной стратегии контента
- 📝 **Генерация уникального контента** - Посты для каждого рабочего дня
- ✏️ **Редактирование** - Полная возможность редактирования перед публикацией
- 🖼️ **Планирование медиа** - Описания для изображений и видео
- 📅 **Прямое планирование** - Сохранение в общий планировщик
- 📥 **Экспорт CSV** - Скачивание плана для работы в Excel/Google Sheets

## 🚀 Быстрый старт

### Предварительные требования

Перед началом убедитесь, что у вас установлено:
- Python 3.8+ 
- pip (менеджер пакетов Python)
- Git (для клонирования репозитория)

### 0. Подготовка (если ещё не сделано)

```bash
# Клонируйте репозиторий
git clone https://github.com/Retyreg/VYUD-AI-Scheduler.git

# Перейдите в директорию проекта
cd VYUD-AI-Scheduler

# Установите зависимости
pip install -r requirements.txt
```

### 1. Настройка API ключа

**Где:** В директории проекта `VYUD-AI-Scheduler`

Создайте файл `.streamlit/secrets.toml`:

**Вариант A - Через терминал (Mac/Linux):**
```bash
# Убедитесь, что вы в директории VYUD-AI-Scheduler
pwd

# Создайте директорию и файл
mkdir -p .streamlit
echo 'GEMINI_API_KEY = "ваш_api_ключ_здесь"' > .streamlit/secrets.toml
```

**Вариант B - Через терминал (Windows):**
```cmd
# Убедитесь, что вы в директории VYUD-AI-Scheduler
cd

# Создайте директорию и файл
mkdir .streamlit
echo GEMINI_API_KEY = "ваш_api_ключ_здесь" > .streamlit\secrets.toml
```

**Вариант C - Вручную:**
1. В директории `VYUD-AI-Scheduler` создайте папку `.streamlit` (если её нет)
2. В папке `.streamlit` создайте файл `secrets.toml`
3. Откройте файл в текстовом редакторе и добавьте:
```toml
GEMINI_API_KEY = "ваш_api_ключ_здесь"
```
4. Замените `ваш_api_ключ_здесь` на ваш реальный API ключ

**Получить API ключ можно здесь:** https://makersuite.google.com/app/apikey

### 2. Запуск приложения

**Где выполнять:** В терминале, в директории `VYUD-AI-Scheduler`

```bash
# Проверьте, что вы в правильной директории
# Вы должны видеть файл streamlit_app.py
ls streamlit_app.py  # Mac/Linux
dir streamlit_app.py # Windows

# Запустите приложение
streamlit run streamlit_app.py
```

**Что происходит после запуска:**
1. Streamlit запустит локальный сервер
2. В терминале появится сообщение:
   ```
   You can now view your Streamlit app in your browser.
   Local URL: http://localhost:8501
   ```
3. Браузер автоматически откроется по адресу http://localhost:8501
4. Если не открылся — откройте браузер вручную и перейдите на http://localhost:8501

**Для остановки приложения:** Нажмите `Ctrl+C` в терминале где запущен Streamlit

### 3. Использование

1. В боковом меню выберите **"AI Strategy Generator"**
2. Заполните форму:
   - **Тема/Продукт**: Опишите ваш продукт или бизнес
   - **Целевая аудитория**: Кто ваши клиенты
   - **Год и Месяц**: Период для генерации плана
   - **Контекстные материалы**: Дополнительная информация (опционально)
3. Нажмите **"Сгенерировать план"**
4. Подождите ~30-60 секунд пока AI генерирует план
5. Отредактируйте посты при необходимости
6. Добавьте ссылки на медиа-файлы
7. Нажмите **"Approve & Schedule"** для сохранения

## 📋 Пример использования

### Входные данные

**Тема/Продукт:**
```
VYUD AI - конструктор онлайн-курсов с ИИ. 
Помогаем экспертам создавать структурированные курсы за 24 часа.
```

**Целевая аудитория:**
```
Предприниматели, эксперты и коучи, которые хотят монетизировать свои знания 
через онлайн-курсы. Возраст 30-50 лет.
```

**Контекстные материалы:**
```
Tone of voice: профессиональный, но дружелюбный. 
Используем реальные кейсы клиентов.
Главные боли: не знают с чего начать, боятся технологий.
```

### Результат

AI сгенерирует ~21 пост (все рабочие дни месяца) с:
- Разнообразным контентом (советы, кейсы, вопросы, истории)
- Стратегией публикаций (например, 3 дня ценности + 1 день продажи)
- Детальными описаниями для медиа
- Готовыми текстами, адаптированными под LinkedIn

### Пример сгенерированного поста

```json
{
  "date": "2026-03-02",
  "platform": "LinkedIn",
  "content_text": "🎯 5 признаков того, что пора создать свой онлайн-курс...",
  "media_type": "image",
  "media_description": "Инфографика с 5 пунктами. Яркие иконки. Цвета: синий и оранжевый",
  "media_url": ""
}
```

## 🎨 Типы медиа

AI генерирует описания для следующих типов медиа:

- **image** - Статичное изображение (инфографика, цитаты, etc.)
- **video** - Видео контент
- **carousel** - Несколько изображений (для Instagram)
- **none** - Только текст, без медиа

## ✏️ Редактирование плана

После генерации вы можете:

1. **Изменить текст поста** - Кликните на ячейку и редактируйте
2. **Изменить дату** - Перенести пост на другой день
3. **Сменить платформу** - LinkedIn → Telegram и наоборот
4. **Добавить медиа** - Вставьте ссылку на Google Drive, S3, Dropbox
5. **Удалить посты** - Нажмите "-" для удаления строки
6. **Добавить посты** - Нажмите "+" для добавления новой строки

## 💾 Сохранение и экспорт

### Approve & Schedule

Кнопка **"Approve & Schedule"** сохраняет все посты в планировщик с статусом "scheduled". После этого посты можно увидеть во вкладке "View History".

### Скачать CSV

Кнопка **"Скачать CSV"** экспортирует план в CSV файл для работы в:
- Microsoft Excel
- Google Sheets
- Notion
- Любой другой инструмент

## 🔧 Устранение неполадок

### "GEMINI_API_KEY не настроен"

**Решение:** Создайте файл `.streamlit/secrets.toml` и добавьте ваш API ключ.

### Команда `streamlit` не найдена

**Проблема:** При запуске `streamlit run streamlit_app.py` появляется ошибка:
- `command not found: streamlit` (Mac/Linux)
- `'streamlit' is not recognized...` (Windows)

**Решение:**
```bash
# Установите streamlit
pip install streamlit

# Или переустановите все зависимости
pip install -r requirements.txt
```

### Ошибка "No module named 'streamlit'" или другие импорты

**Проблема:** Python не находит установленные модули

**Решение:**
```bash
# Убедитесь, что используете правильный pip
pip --version

# Переустановите зависимости
pip install -r requirements.txt

# Если используете виртуальное окружение, активируйте его:
# Mac/Linux:
source venv/bin/activate
# Windows:
venv\Scripts\activate
```

### Приложение не запускается, ошибка импорта

**Проблема:** `ModuleNotFoundError: No module named 'google.generativeai'`

**Решение:**
```bash
# Установите конкретно этот пакет
pip install google-generativeai

# Или все зависимости заново
pip install -r requirements.txt
```

### Не знаю, в какой директории я нахожусь

**Проблема:** Не понятно, в правильной ли директории выполняется команда

**Решение:**
```bash
# Узнайте текущую директорию:
# Mac/Linux:
pwd
# Windows:
cd

# Посмотрите содержимое директории:
# Mac/Linux:
ls
# Windows:
dir

# Вы должны видеть файлы: streamlit_app.py, requirements.txt, README.md

# Если вы не в директории проекта, перейдите в неё:
cd путь/к/VYUD-AI-Scheduler
```

### Браузер не открывается автоматически

**Решение:** 
1. Откройте браузер вручную
2. Перейдите на http://localhost:8501
3. Или используйте адрес из терминала (обычно там указан)

### Порт 8501 уже занят

**Проблема:** `Address already in use` или `Port 8501 is in use`

**Решение:**
```bash
# Вариант 1: Используйте другой порт
streamlit run streamlit_app.py --server.port 8502

# Вариант 2: Найдите и остановите процесс на порту 8501
# Mac/Linux:
lsof -ti:8501 | xargs kill -9
# Windows:
netstat -ano | findstr :8501
# Затем используйте taskkill /PID <номер_процесса> /F
```

### "Модель вернула невалидный JSON"

**Решение:** Это редкая ошибка. Просто нажмите "Сгенерировать план" еще раз. AI иногда может вернуть текст в неожиданном формате.

### Генерация занимает слишком много времени

**Причина:** Генерация 20+ постов требует времени. Обычно 30-60 секунд.

**Совет:** Подождите, процесс идет. Индикатор "AI генерирует..." показывает прогресс.

### Посты не соответствуют ожиданиям

**Решение:** 
1. Добавьте больше деталей в "Контекстные материалы"
2. Укажите tone of voice, примеры, особенности аудитории
3. Используйте редактор для корректировки

## 🤖 Как работает AI

AI использует Chain of Thought подход:

1. **Анализ** - Изучает продукт и аудиторию
2. **Стратегия** - Создает контент-стратегию (например, "3 дня пользы + 1 день продажи")
3. **Генерация** - Создает уникальные посты для каждого дня
4. **Форматирование** - Возвращает в структурированном JSON формате

Модели (в порядке приоритета):
- gemini-2.0-flash-exp (самая быстрая и новая)
- gemini-1.5-pro-latest (более креативная)
- gemini-1.5-pro (стабильная версия)

## 📊 Структура данных

Каждый пост содержит:

```json
{
  "date": "YYYY-MM-DD",           // Дата публикации
  "platform": "LinkedIn",          // Платформа
  "content_text": "...",          // Текст поста
  "media_type": "image",          // Тип медиа
  "media_description": "...",     // Описание для дизайнера
  "media_url": ""                 // Ссылка на файл (заполняется вручную)
}
```

## 💡 Советы по использованию

1. **Будьте конкретны** - Чем больше деталей, тем лучше результат
2. **Укажите tone of voice** - "Профессиональный", "Дружелюбный", "Экспертный"
3. **Добавьте примеры** - "Как у @username" или "Стиль Y Combinator"
4. **Проверяйте факты** - AI может генерировать информацию, всегда проверяйте
5. **Редактируйте** - Используйте AI как черновик, добавляйте свой стиль
6. **Сохраняйте резервные копии** - Скачивайте CSV перед большими изменениями

## 🔒 Безопасность

- API ключ хранится локально в `.streamlit/secrets.toml`
- Файл `secrets.toml` не коммитится в git (в `.gitignore`)
- Данные отправляются только в Google Gemini API
- Сгенерированный контент не сохраняется на серверах Google после генерации

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи в терминале
2. Убедитесь, что API ключ корректный
3. Попробуйте с другими входными данными
4. Создайте issue в GitHub репозитории

## 📚 Дополнительные ресурсы

- [Google AI Studio](https://makersuite.google.com/)
- [Gemini API Documentation](https://ai.google.dev/docs)
- [Streamlit Documentation](https://docs.streamlit.io/)
