# 🚀 Быстрый старт - Где запускать команды?

## ❓ "Эту команду я где запускаю?"

Если вы спрашиваете, **где запускать команду** `streamlit run streamlit_app.py`, вот подробный ответ:

## 📍 ГДЕ: В терминале, в директории проекта

### Что такое терминал?

**На разных операционных системах:**
- **Windows:** Командная строка (cmd) или PowerShell
  - Найти: Нажмите `Win + R`, введите `cmd`, нажмите Enter
  - Или: Найдите "Command Prompt" в меню Пуск
- **Mac:** Terminal (Терминал)
  - Найти: Приложения → Утилиты → Terminal
  - Или: Нажмите `Cmd + Space`, введите "Terminal"
- **Linux:** Terminal
  - Обычно: `Ctrl + Alt + T`

### Пошаговая инструкция

#### 1️⃣ Откройте терминал
Используйте один из способов выше для вашей ОС

#### 2️⃣ Перейдите в директорию проекта

```bash
# Перейдите в папку, куда вы клонировали проект
# Замените путь на ваш реальный путь к проекту
cd /путь/к/VYUD-AI-Scheduler

# Примеры:
# Windows:
cd C:\Users\YourName\Projects\VYUD-AI-Scheduler

# Mac/Linux:
cd ~/Projects/VYUD-AI-Scheduler
```

#### 3️⃣ Убедитесь, что вы в правильной директории

```bash
# Посмотрите содержимое папки
# На Windows:
dir

# На Mac/Linux:
ls

# Вы должны увидеть файлы:
# - streamlit_app.py ← Этот файл должен быть!
# - requirements.txt
# - README.md
# - app.py
# и другие...
```

#### 4️⃣ ТЕПЕРЬ запустите команду

```bash
streamlit run streamlit_app.py
```

## 🎯 Полный пример от начала до конца

### Вариант А: Если проект уже клонирован

```bash
# 1. Откройте терминал
# 2. Перейдите в директорию проекта
cd путь/к/VYUD-AI-Scheduler

# 3. Проверьте, что файл существует
ls streamlit_app.py    # Mac/Linux
dir streamlit_app.py   # Windows

# 4. Запустите приложение
streamlit run streamlit_app.py
```

### Вариант Б: Если начинаете с нуля

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/Retyreg/VYUD-AI-Scheduler.git

# 2. Перейдите в директорию
cd VYUD-AI-Scheduler

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Настройте API ключ
# Создайте файл .streamlit/secrets.toml с вашим GEMINI_API_KEY
# См. подробную инструкцию в README.md

# 5. Запустите приложение
streamlit run streamlit_app.py
```

## ✅ Что должно произойти

После выполнения команды `streamlit run streamlit_app.py`:

1. В терминале появятся сообщения:
```
You can now view your Streamlit app in your browser.

Local URL: http://localhost:8501
Network URL: http://192.168.x.x:8501
```

2. Автоматически откроется браузер с адресом http://localhost:8501

3. Вы увидите приложение "Post History Manager 🚀"

4. В боковом меню будут опции:
   - Create Post
   - View History
   - **AI Strategy Generator** ← Новая функция!

## ❌ Частые ошибки

### Ошибка: "streamlit: command not found"

**Причина:** Streamlit не установлен

**Решение:**
```bash
pip install streamlit
# Или установите все зависимости:
pip install -r requirements.txt
```

### Ошибка: "No such file or directory: streamlit_app.py"

**Причина:** Вы находитесь не в той директории

**Решение:**
```bash
# Проверьте, где вы находитесь
pwd  # Mac/Linux
cd   # Windows

# Перейдите в правильную директорию
cd путь/к/VYUD-AI-Scheduler

# Проверьте наличие файла
ls streamlit_app.py  # Mac/Linux
dir streamlit_app.py # Windows
```

### Ошибка: "GEMINI_API_KEY не настроен"

**Причина:** Не создан файл с API ключом

**Решение:** См. раздел "Настройка API ключа" в [README.md](README.md) или [AI_STRATEGY_GUIDE.md](AI_STRATEGY_GUIDE.md)

## 📚 Дополнительная информация

- **Полная документация:** [README.md](README.md)
- **Руководство по AI Strategy Generator:** [AI_STRATEGY_GUIDE.md](AI_STRATEGY_GUIDE.md)
- **Визуальная документация UI:** [UI_MOCKUP.md](UI_MOCKUP.md)

## 🆘 Всё ещё не работает?

1. Убедитесь, что Python установлен: `python --version` или `python3 --version`
2. Убедитесь, что pip работает: `pip --version`
3. Проверьте, что вы в правильной директории: `pwd` (Mac/Linux) или `cd` (Windows)
4. Убедитесь, что файл streamlit_app.py существует: `ls` или `dir`
5. Переустановите зависимости: `pip install -r requirements.txt`

Если проблема остаётся, откройте Issue в GitHub репозитории с описанием ошибки.

---

**Кратко:** Команду `streamlit run streamlit_app.py` нужно запускать в **терминале**, находясь **в директории VYUD-AI-Scheduler** (там где находится файл streamlit_app.py).
